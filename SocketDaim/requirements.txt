# SocketDaim Gateway - Python Dependencies
# Python 3.11+ required

# === TCP / Networking ===
# asyncio (stdlib) - TCP server/client
# struct   (stdlib) - binary framing

# === Database ===
asyncpg>=0.30.0             # PostgreSQL async driver (used by both gateways)

# === Serialization ===
pydantic>=2.7.0             # data classes + JSON
pydantic-settings>=2.3.0    # env-var driven settings

# === Outbox / Local Cache (Egress only) ===
aiosqlite>=0.20.0           # SQLite async driver for the outbox pattern

# === Egress target: LOAS-side MariaDB (Egress only) ===
aiomysql>=0.2.0             # MariaDB/MySQL async driver (pure-Python; PyMySQL 기반)

# === Logging ===
structlog>=24.1.0           # structured logging

# === HTTP server (Admin UI on port 9108; Egress healthcheck) ===
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.9

# === Build backend for gw_proto ===
hatchling>=1.27.0

# === Testing ===
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# === Dev Tools ===
ruff>=0.5.0
mypy>=1.10.0
